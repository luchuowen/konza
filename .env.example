# Firebase / Firestore client config — placeholders only. Real values are
# provisioned in a separate, explicitly-requested step after Session 9 (see
# docs/BLUEPRINT.md "What happens after Session 9"). Never commit a filled-in
# .env.local.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# firebase-admin (server actions only — service account, never exposed to the client)
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=

# Sales inbox notified on every lead submission (docs/KONZA_SPEC.md §7).
# [CONFIRM] — email recipient still pending Konza's confirmation, see
# docs/KONZA_SPEC.md §10, item 2.
SALES_NOTIFICATION_EMAIL=
